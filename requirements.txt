# PACS Admin Tool - Python dependencies
#
# Install all at once:
#   pip install -r requirements.txt
#
# Core DICOM
pydicom>=2.4.0
pynetdicom>=2.0.0

# HL7
hl7>=0.4.0

# Web server (web version only)
flask>=3.0.0
flask-socketio>=5.3.0
simple-websocket>=1.0.0

# Build desktop .exe (optional, Windows only)
pyinstaller>=6.0.0
