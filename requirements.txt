# PACS Admin Tool - Python dependencies
#
# Install all at once:
#   pip install -r requirements.txt
#
# Versions are pinned to compatible-release ranges (~=) so that
# patch/minor updates are allowed but major-version bumps are not.
#
# Core DICOM
pydicom~=3.0
pynetdicom~=3.0

# HL7
hl7~=0.4

# Web server (web version only)
flask~=3.1
flask-socketio~=5.6
simple-websocket~=1.1
pyyaml~=6.0

# System tray icon
pystray~=0.19
Pillow~=12.2

# Build desktop .exe (optional, Windows only)
pyinstaller~=6.19
